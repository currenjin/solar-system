{
  "name": "solarsystem",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx parcel src/index.html",
    "build": "npx parcel build src/index.html",
    "predeploy": "rm -rf dist && npx parcel build src/index.html --no-source-maps --public-url ./",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.6.2",
    "three": "^0.133.0"
  },
  "devDependencies": {
    "@parcel/transformer-image": "^2.0.0-rc.0",
    "gh-pages": "^4.0.0"
  }
}
